import { MyList } from 'app/export-components/List';
import { ExportComponents } from '@unified-tasklist/common/types/';

export const exportComponents: ExportComponents.ListCollection = {
  List: MyList,
};

export const List = MyList;
